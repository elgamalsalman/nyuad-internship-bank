// --- imports ---

// --- helper functions ---

// TODO: get_user_sheet()
const get_user_sheet = async (netid) => {
	return undefined;
};

// TODO: get_user_email(), query database instead
const get_user_email = async (netid) => {
	return `${netid}@nyu.edu`;
};

// --- exports ---

export default {
	get_user_sheet,
	get_user_email,
};
